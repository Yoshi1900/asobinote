   <script>
      $('#star-rate<%= playground.id%>').raty({
        size      : 36,
        starOff   : '<%= asset_path('star-off.png') %>',
        starOn    : '<%= asset_path('star-on.png') %>',
        starHalf: '<%= asset_path('star-half.png') %>',
        half: true,
        readOnly: true, 
        score: <%= playground.posts.average(:star).to_f.round(1) %>,
        //↑ 平均点を算出し、round関数で切り上げる
      });
    </script>